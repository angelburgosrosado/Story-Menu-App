{
  "entities": {
    "User": {
      "title": "User",
      "description": "User registration profile",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Firebase UID of user" },
        "email": { "type": "string", "description": "Email address of user" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of profile creation" },
        "tier": { "type": "string", "description": "Active subscription tier name, e.g. Freelance, Pro, Enterprise" },
        "subscriptionId": { "type": "string", "description": "Active checkout or billing identifier reference" },
        "paymentMethod": { "type": "string", "description": "Source checkout choice, e.g. Stripe, PayPal" }
      },
      "required": ["id", "email"]
    },
    "Project": {
      "title": "Project",
      "description": "A generated multi-page comic book project",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier of project" },
        "userId": { "type": "string", "description": "Owner identifier of project" },
        "title": { "type": "string", "description": "Title of the comic book issue" },
        "genre": { "type": "string", "description": "Comic genre" },
        "language": { "type": "string", "description": "Interface and storytelling language" },
        "comicFaces": { "type": "string", "description": "Escaped JSON serialization of all panels, dialogues, image links, and choices" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Creation timestamp" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Update timestamp" }
      },
      "required": ["id", "userId", "title"]
    },
    "Character": {
      "title": "Character",
      "description": "Custom drafted characters in user's creative bible",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique character code" },
        "userId": { "type": "string", "description": "Owner creator id" },
        "name": { "type": "string", "description": "Character's name" },
        "roleType": { "type": "string", "enum": ["Hero", "Co-Star", "Villain"], "description": "Multiverse archetype role" },
        "description": { "type": "string", "description": "Appearance and traits bible" },
        "imageUrl": { "type": "string", "description": "Base64 or URL linking profile photo" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "name", "roleType"]
    },
    "Draft": {
      "title": "Draft",
      "description": "An unfinished workspace draft containing entire character setup, comicFaces, and storyBlueprint arrays",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier of draft" },
        "userId": { "type": "string", "description": "Owner identifier of draft" },
        "title": { "type": "string", "description": "User supplied title of the draft workspace" },
        "genre": { "type": "string", "description": "Active story genre tag" },
        "comicFaces": { "type": "string", "description": "Escaped JSON serialization of unfinished comicFaces state" },
        "storyBlueprint": { "type": "string", "description": "Escaped JSON serialization of the storyBlueprint timeline array" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "title"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": "User",
      "description": "Collection of creators"
    },
    "users/{userId}/projects/{projectId}": {
      "schema": "Project",
      "description": "Subcollection under user containing comic book folders"
    },
    "users/{userId}/characters/{characterId}": {
      "schema": "Character",
      "description": "Subcollection under user containing cast bibles"
    },
    "users/{userId}/drafts/{draftId}": {
      "schema": "Draft",
      "description": "Subcollection under user containing unfinished work-in-progress drafts"
    }
  }
}
